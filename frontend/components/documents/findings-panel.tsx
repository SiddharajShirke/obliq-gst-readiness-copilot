"use client";

import { AlertTriangle, CheckCircle2, Play } from "lucide-react";
import { useEffect,useState } from "react";
import { toast } from "sonner";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
import { apiFetch } from "@/lib/api";
import type { Finding } from "@/lib/types";

export function FindingsPanel({applicationId,onChanged}:{applicationId:string;onChanged:()=>void}){const [findings,setFindings]=useState<Finding[]>([]);const [busy,setBusy]=useState(false);async function load(){setFindings(await apiFetch<Finding[]>(`/applications/${applicationId}/findings`))}useEffect(()=>{load().catch(()=>undefined)},[applicationId]);async function run(){setBusy(true);try{const result=await apiFetch<{finding_count:number;findings:Finding[]}>(`/applications/${applicationId}/validate`,{method:"POST"});setFindings(result.findings);toast.success(`${result.finding_count} validation findings generated`);onChanged();}catch(error){toast.error(error instanceof Error?error.message:"Validation failed")}finally{setBusy(false)}}async function resolve(id:string,status:"resolved"|"accepted"){try{await apiFetch(`/findings/${id}/resolve`,{method:"POST",body:JSON.stringify({status})});await load();onChanged();toast.success(status==="resolved"?"Finding resolved":"Finding accepted for review") }catch(error){toast.error(error instanceof Error?error.message:"Unable to update finding")}}const open=findings.filter(item=>item.status==="open");return <div className="grid gap-5"><Card className="p-5"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-center"><div><h3 className="font-bold">Deterministic GST validation</h3><p className="mt-2 text-sm text-[#6b6562]">Checks GSTIN format, dates, tax arithmetic, selected period, client ownership and duplicates.</p></div><Button onClick={run} disabled={busy}><Play size={16}/>{busy?"Checking…":"Run all checks"}</Button></div><div className="mt-6 grid gap-3 sm:grid-cols-3"><div className="rounded-2xl bg-[#f8f7f5] p-4"><p className="text-2xl font-bold">{findings.length}</p><p className="text-xs text-[#77716e]">Total findings</p></div><div className="rounded-2xl bg-red-50 p-4"><p className="text-2xl font-bold text-red-700">{open.length}</p><p className="text-xs text-red-700">Open findings</p></div><div className="rounded-2xl bg-emerald-50 p-4"><p className="text-2xl font-bold text-emerald-700">{findings.length-open.length}</p><p className="text-xs text-emerald-700">Resolved / accepted</p></div></div></Card><Card className="overflow-hidden"><div className="border-b border-[#eeeae6] p-5"><h3 className="font-bold">Review queue</h3></div>{findings.length?<div className="divide-y divide-[#eeeae6]">{findings.map(item=><div key={item.id} className="p-5"><div className="flex items-start gap-3"><span className={`grid h-9 w-9 shrink-0 place-items-center rounded-xl ${item.status==="open"?"bg-amber-50 text-amber-700":"bg-emerald-50 text-emerald-700"}`}>{item.status==="open"?<AlertTriangle size={17}/>:<CheckCircle2 size={17}/>}</span><div className="flex-1"><div className="flex flex-wrap items-center gap-2"><strong className="text-sm">{item.message}</strong><Badge value={item.status}/><Badge value={item.severity}/></div><p className="mt-2 text-xs text-[#77716e]">{item.finding_type.replaceAll("_"," ")}{item.details&&Object.keys(item.details).length?` · ${JSON.stringify(item.details)}`:""}</p>{item.status==="open"&&<div className="mt-4 flex gap-2"><Button variant="secondary" className="min-h-8 px-3 py-1 text-xs" onClick={()=>resolve(item.id,"resolved")}>Mark resolved</Button><Button variant="ghost" className="min-h-8 px-3 py-1 text-xs" onClick={()=>resolve(item.id,"accepted")}>Accept for review</Button></div>}</div></div></div>)}</div>:<div className="p-10 text-center text-sm text-[#77716e]">No validation findings yet.</div>}</Card></div>}
